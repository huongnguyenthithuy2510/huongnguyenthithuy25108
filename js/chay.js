var typed = new Typed(".auto-type", {
    strings: ["Programmer", "Designer", "YouTuber"], // Các chuỗi sẽ được gõ
    typeSpeed: 150,  // Tốc độ gõ (ms)
    backSpeed: 150,   // Tốc độ xóa (ms)
    loop: true        // Lặp lại hiệu ứng
});